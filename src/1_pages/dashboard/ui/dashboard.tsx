import { Sidebar } from "@widgets/sidebar";
import { FC } from "react";

const Dashboard = () => {
  return (
    <div className="bg-muted h-screen w-full p-2 flex flex-row gap-4">
      <div className="rounded-md bg-background h-full w-full">dashboard</div>
    </div>
  );
};

export default Dashboard;
